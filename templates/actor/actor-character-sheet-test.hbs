<form class="{{cssClass}} flexcol" autocomplete="off">
  
  {{!-- Header --}}
  <header class="sheet-header">
    <div class="header-content">
      <div class="name-section">
        <input name="name" type="text" value="{{actor.name}}" placeholder="Character Name" class="character-name"/>
        <input name="system.profile.concept" type="text" value="{{system.profile.concept}}" placeholder="Concept" class="character-concept"/>
      </div>
    </div>
  </header>

  <div class="sheet-main">
    {{!-- Left Sidebar --}}
    <aside class="sidebar">
      
      {{!-- Portrait --}}
      <section class="portrait-section">
        <div class="portrait-frame">
          <img src="{{actor.img}}" alt="{{actor.name}}" class="character-portrait" title="Click to change portrait">
          <div class="portrait-overlay">
            <i class="fas fa-camera"></i>
          </div>
        </div>
      </section>

      {{!-- Wounds --}}
      <section class="wounds">
        <h3>HERIDAS</h3>
        <div class="wound-type">
          <label>Minor:</label>
          <div class="wound-pips">
            {{#each (range 5) as |i|}}
              <input type="checkbox" class="wound-pip minor" name="system.wounds.minor" value="{{add i 1}}" 
                     {{#if (lte (add i 1) system.wounds.minor)}}checked{{/if}}>
            {{/each}}
          </div>
          <span class="wound-penalty">(-{{system.wounds.minor}})</span>
        </div>
      </section>

    </aside>

    {{!-- Main Content --}}
    <section class="main-content">
      <p>TEST: Basic template rendering</p>
      <p>Actor Name: {{actor.name}}</p>
      <p>Actor Type: {{actor.type}}</p>
      <p>System Data: {{#if system}}Available{{else}}Missing{{/if}}</p>
    </section>
  </div>

</form>
